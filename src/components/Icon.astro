---
export interface Props{
  icon: string
}
const { icon, ...attributes } = Astro.props as Props
const { default: component } = await import(`../../public/skills-icons/${icon}.astro?raw`)
---

<Fragment set:html={component} {...attributes} />