---
interface Props {
	title?: string;
	body?: string;
	href?: string;
}

const { href, title, body } = Astro.props;
---
<article class="group bg-bg-primary rounded-sm flex p-4 flex-col items-center border border-black/50 gap-4 text-secondary w-auto h-auto sm:h-[200px] hover:scale-105 hover:shadow-lg hover:shadow-black/50 transition-all">
	<slot/>
</article>
<style>
	
</style>
